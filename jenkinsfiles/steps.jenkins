#!groovy

properties([disableConcurrentBuilds()])

pipeline{
    agent{
        label 'master'
        }
    stages{
        stage("Test product page"){
            steps {
                bat 'pytest test_product_page.py'
            }
        }
        stage("Test main page"){
            steps {
                bat 'pytest test_main_page.py'
            }
        }
    }
}