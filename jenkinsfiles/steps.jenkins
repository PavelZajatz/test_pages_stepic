#!groovy

properties([disableConcurrentBuilds()])

pipeline{
    agent{
        label 'master'
        }
    stages{
        stage("Test main page"){
            steps {
                sh 'pytest test_main_page.py'
            }
        }
        stage("Test product page"){
            steps {
                sh 'pytest test_product_page.py'
            }
        }
    }
}